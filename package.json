{
  "name": "type-coverage-diff",
  "version": "0.1.0",
  "description": "CLI to compare TypeScript any coverage between two git refs",
  "license": "MIT",
  "type": "module",
  "bin": {
    "type-coverage-diff": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc",
    "test": "node --import tsx/esm --test test/**/*.test.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^14.0.0",
    "type-coverage-core": "^2.29.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0"
  }
}
